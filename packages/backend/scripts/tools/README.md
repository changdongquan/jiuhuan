# backend/scripts/tools

此目录用于存放 **一次性/辅助脚本**（不参与后端服务运行）。

约定：

- 后端运行时代码仍在 `backend/server.js`、`backend/routes/` 等目录，**不要**把运行时代码放进本目录。
- 如脚本需要读取后端配置/数据库连接，请使用相对路径引用：
  - `../../config`
  - `../../database`
- 如脚本需要读取仓库根目录下的参考 SQL，请使用 `../../../sql/...`。
