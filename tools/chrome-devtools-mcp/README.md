# chrome-devtools-mcp (local)

This folder installs `chrome-devtools-mcp` locally (so we don't need a global install).

## Quick start

```bash
cd tools/chrome-devtools-mcp
npm install
npm run mcp -- --help
```

